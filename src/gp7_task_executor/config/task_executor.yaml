# Default parameters for gp7_task_executor (C++ task executor node)

/**:
  ros__parameters:
    move_group_name: "arm"
    base_frame: "world"
    ee_link: "tool0"
    planning_time: 2.0
    num_planning_attempts: 5
    max_velocity_scaling_factor: 0.5
    max_acceleration_scaling_factor: 0.5
