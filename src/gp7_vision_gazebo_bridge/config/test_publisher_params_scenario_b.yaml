# Scenario B: different footprint / height.
# Center z = 0.44 m, height 0.08 m → bottom at 0.44 - 0.04 = 0.40 m (table surface).

box_test_publisher_node:
  ros__parameters:
    pose_topic: "/vision/box_pose"
    size_topic: "/vision/box_size"
    frame_id: "world"

    x: 0.0
    y: -0.7
    z: 0.44
    yaw_rad: 0.0

    length_m: 0.12
    width_m: 0.06
    height_m: 0.08

    publish_rate_hz: 1.0
    log_each_publish: false
